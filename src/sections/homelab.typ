#import "../template.typ": *
#custombox(colorProjects)[
  #cvproject(
    [#fa-server() Self-Hosted Infrastructure & Networking],
    "2024 - Present",
    colorProjects,
  )[
    #list(
      [Administering a *Debian-based homelab* running *Docker/Portainer* to deploy and manage self-hosted privacy services (Nextcloud, Immich, SearXNG).],
      [Configured an *NGINX reverse proxy* for secure external access and implemented an automated *NAS backup strategy*.]
    )
  ]
]
